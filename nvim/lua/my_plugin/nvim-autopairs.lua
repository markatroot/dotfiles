-- Unused: nvim-autopairs isn't in the plugin list (init.lua / lua/custom/pack),
-- so this config is never loaded. Kept for reference.
require('nvim-autopairs').setup({
  map_cr = true,
  map_bs = true,
  enable_afterquote = false,
})

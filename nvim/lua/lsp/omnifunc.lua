-- Placeholder for custom omnifunc completion logic; currently unused
-- (nothing is added to M). Neovim's built-in LSP omnifunc is wired up
-- directly in plugin/lsp.lua instead.
local U = require 'config.utils'
local util = vim.lsp.util
local M = {}

return M
